# CallSense frontend (Next.js) — Phase 5

Reserved. The role-aware dashboards (Director / Team Leader / Advisor), the
audio-synced call-detail page, and the dispute UI are built in the frontend
phase. The `web` service in `docker-compose.yml` is enabled then.

Stack: Next.js 14 + TypeScript + Tailwind + shadcn/ui + Recharts.
