"""Ingestion package (Phase 2+): the CallEnvelope canonical schema and the
SourceAdapter interface with folder / REST / mock-CRM implementations. A new
telephony source plugs in as one new adapter with zero pipeline changes."""
