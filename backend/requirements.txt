# Runtime dependencies (Phase 1). Heavy ML deps (faster-whisper, pyannote,
# spaCy, anthropic) are added in the phases that use them to keep the Phase-1
# image/env small and fast to build.
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
