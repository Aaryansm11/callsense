"""Pipeline package (Phase 2+): Postgres-backed job queue, worker loop, and the
staged processors (transcribe -> diarise -> redact -> classify -> analyse ->
validate). Placeholder in Phase 1; the schema it writes to already exists."""
